/**
 * Standard-Fahrerkader: Porsche GT Cup
 *
 * Diese Liste bleibt bewusst leer. Später können hier feste Ausgangsdaten
 * hinterlegt werden. Änderungen in der Oberfläche werden pro Liga getrennt
 * im Browser gespeichert.
 */

export const DEFAULT_DRIVERS = Object.freeze([]);
